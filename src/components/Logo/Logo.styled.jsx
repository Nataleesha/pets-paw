import { styled } from "styled-components";

export const LogoGroup = styled.div`
  display: inline-flex;
  flex-direction: row;
  align-items: center;
  gap: 8px;
`;

export const LogoName = styled.p`
  color: var(--text-dark);
  font-size: 24px;
  font-weight: 700;
`;
